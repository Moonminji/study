Program = 명령 집합으로 하드디스크에 저장된 파일 중 실행 가능한 파일.

Process = 실행 중인 명령 집합으로 메모리에 로딩된 프로그램, 즉 실행된 프로그램
  - 대화형 프로세스 = 프로그램과 정보를 주고받는 동안 Foreground에서 실행되며 입출력도 이뤄짐, 만약 터미널 상에서 입출력 작업이 필요하지 않으면 Background 상태에서 작업 수행 가능.
  - 배치 프로세스 = 일련의 작업을 몰아서 특정 시간에 실행시키는 것, 일반적으로 터미널과의 입출력 교류가 전혀 없는 프로세스.    ex) batch, at

Daemon = 특정 서비스를 위해 백그라운드 상태에서 계속 실행되는 서버 프로세스


[ Process와 Daemon의 차이]

일반 process는 실행 상태에 들어가 일련의 명령을 수행하고 명령이 끝나면 process가 소멸됨.
Daemon process는 일련의 명령이 끝나도 소멸되지 않고 메모리 상에 상주하면서 특정 조건이 되면 다시 명령을 수행함.
